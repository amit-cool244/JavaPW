public class Datatypes
{
    public static void main(String[] args)
     {
        float info = 45.5f;
        System.out.println(info);
     }
}

// By default Java cmpiler is designed in such a way that it considers decimal numbers as double even though it is float, we need to specify with the suffix "f" if we want to use float value example float data = 45.5f;